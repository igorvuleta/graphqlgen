query FindCustomerPerId($id: ID!) {
  customer(id: $id) {
    address
    city
    companyName
    contactTitle
    country
    customerId
    fax
    phone
    postalCode
    customerId
    region
  }
}
